/*
 * Keyframe — animation value at a point in time with interpolation.
 *
 * This is a header-only template implementation; Keyframe.h contains all definitions.
 * This .cpp file exists to satisfy the CMake build system.
 */

#include "Keyframe.h"

// All Keyframe functionality is implemented inline in Keyframe.h
// No additional out-of-line definitions needed.