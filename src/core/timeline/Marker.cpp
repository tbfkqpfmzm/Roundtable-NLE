/*
 * Marker — timeline marker/annotation at a specific time position.
 *
 * This is a header-only implementation; Marker.h contains all definitions.
 * This .cpp file exists to satisfy the CMake build system.
 */

#include "Marker.h"

// All Marker functionality is implemented inline in Marker.h
// No additional out-of-line definitions needed.